{
  "name": "agentheim-bridge",
  "displayName": "Agentheim Bridge",
  "description": "Local 127.0.0.1 bridge that lets the Agentheim dashboard open a real, interactive Claude terminal inside VS Code (ADR-0018). Loopback-only + per-activation shared-secret token; opt-in, off-by-default permission-bypass on POST /run via strict-true skipPermissions.",
  "version": "0.2.1",
  "publisher": "agentheim",
  "private": true,
  "license": "SEE LICENSE IN ../LICENSE",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./extension.js",
  "contributes": {},
  "scripts": {
    "test": "node --test --test-concurrency=1",
    "package": "vsce package"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.0.0"
  }
}
